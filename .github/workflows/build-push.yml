name: Slurm Matrix Build and Push

on:
  push:
    branches:
      - "main"
      - "dev"
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform: [linux/amd64, linux/arm64]
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          install: true

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Images
        uses: docker/bake-action@v6
        with:
          source: "schedmd/slurm/ubuntu-24.04/compose.yaml"
          push: true
          set: |
            mariadb.tags=ghcr.io/${{ github.repository_owner }}/mariadb:latest
            slurmdbd.tags=ghcr.io/${{ github.repository_owner }}/slurmdbd:latest
            slurmctld.tags=ghcr.io/${{ github.repository_owner }}/slurmctld:latest
            slurmd.tags=ghcr.io/${{ github.repository_owner }}/slurmd:latest
            slurmrestd.tags=ghcr.io/${{ github.repository_owner }}/slurmrestd:latest
            sackd.tags=ghcr.io/${{ github.repository_owner }}/sackd:latest

